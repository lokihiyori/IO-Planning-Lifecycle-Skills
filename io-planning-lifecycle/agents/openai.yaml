interface:
  display_name: "IO Planning Lifecycle"
  short_description: "Create visual, bilingual, versioned IO Flow specs"
  default_prompt: "Use $io-planning-lifecycle to turn these requirements into a visual, versioned IO Flow in English or Chinese."
